// import moment from 'moment';

export default {
  appVersion: '1.0.1',
  appBuildDate: 'Mar 12, 2020',
  // appBuildDate: process.env.NODE_ENV ? moment(new Date()).format('MMM DD, YYYY, h:mm a') : '',
};
